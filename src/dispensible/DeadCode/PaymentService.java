package dispensible.DeadCode;

public class PaymentService {
    public void processPayment(double amount) {
        System.out.println("Processing payment of $" + amount);
    }
    // printDebugInfo() removed — dead code!
}
